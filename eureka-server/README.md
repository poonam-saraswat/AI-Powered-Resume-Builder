# eureka-server
Service registry for ResumeAI. All services register here on startup so they can find each other by name (e.g. `auth-service`) instead of hardcoded URLs.
- Port: **8761**
- UI: http://localhost:8761
- No DB, no auth.
